#include "Graph.hpp"

Graph::Graph(vector <Arc*> list_of_arcs){

    Arc* aux;
    int c = -1; 
    int previous = list_of_arcs.at(0)->getFst() -1; 
    for(int i=0; i < list_of_arcs.size(); i++){ 
        aux = list_of_arcs.at(i);
    }
}
